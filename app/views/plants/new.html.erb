<%= simple_form_for @plant do |f| %>
   <%= f.input :name, label: "Plant Name", placeholder: "Enter plant name"%>
  <%= f.input :description %>
  <%= f.input :price, as: :integer%>
  <%= f.input :photo, as: :file %>
 <%= f.button :submit, "Create Plant", class: "btn-success" %>
<% end %>
<div class='container'>
<%= link_to "<<-- Back home", '/', class: "btn btn-secondary mb-2 text-center" %>
</div>
<div class='container'>
<%= link_to "<<-- Back to List", plants_path, class: "btn btn-primary mb-2 text-center" %>
</div>
